# Проект: Gesture Detection

![Обложка](./images/photo_1.png)

🔗 [Видео презентация](https://kirillshiryaev61.github.io/gesture_detection/video_presentation.MP4)

### Описание проекта:

Проект реализует распознавание жестов руки в реальном времени с помощью веб-камеры — аналогично некоторым функциям на iPhone. Цель — изучение работы с библиотеками `OpenCV` и `MediaPipe`.

Скрипт отслеживает 5 жестов:

- `thumb_up` — 👍
- `thumb_down` — 👎
- `peace` — ✌️
- `ok` — 👌
- `heart` — 🫰

### Как это работает?

1. Камера захватывает видеопоток.
2. `MediaPipe` детектирует руку и возвращает 21 ключевую точку.
3. Координаты нормализуются относительно запястья.
4. Полученный вектор подаётся в предобученную модель `LogisticRegression`.
5. Если уверенность модели > `0.95` — название жеста отображается в левом верхнем углу кадра.

### Модель
- Обучена на данных, собранных вручную, `~300-400` примеров на жест и `~1300` примеров негативного класса (нет жеста);
- Использованы только нормализованные координаты ключевых точек.
  
#### Метрики на тестовой выборке:
```txt
Precision (weighted) — 0.97
Recall (weighted)    — 0.96
F1-score (weighted)  — 0.96
```  

### Требуемые зависимости:

```txt
joblib==1.5.0
mediapipe==0.10.31
numpy==2.2.6
opencv-python==4.12.0.88
```

### Установка:

```bash
git clone https://github.com/KirillShiryaev61/gesture_detection.git
```

### Запуск:

```bash
# если используешь venv
source myenv/bin/activate        # Linux/macOS
myenv\Scripts\activate           # Windows

# если используешь conda
conda activate myenv

cd gesture_detection
python main.py
```

### Технологии:

#### Обучение модели:
- **Data Processing:** `numpy`, `pandas`;
- **Visualization:** `matplotlib`, `plotly`;
- **ML & Preprocessing:** `scikit-learn`, `skops`.

#### Инференс модели:
- **Data Processing:** `numpy`;
- **ML & Preprocessing:** `mediapipe`, `opencv`.